from distutils.core import setup

setup(name='solartherm',
		version='${PACKAGE_VERSION}',
		package_dir={'': '${CMAKE_CURRENT_SOURCE_DIR}'},
		packages=['solartherm'],
		#scripts=['scripts/st_wea_to_mo'],
		)
